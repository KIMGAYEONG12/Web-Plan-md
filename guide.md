# [개발 기획서] WebToDo - 웹페이지 할 일(TODO) 저장 확장 프로그램

## ① 서비스 정의

- **서비스명:** WebToDo (웹투두)
- **타겟 유저:** 웹서핑이나 인터넷 강의 수강, 자료 조사를 하면서 동시에 할 일(Task)을 관리하고 싶은 모든 사용자
- **기획 배경 및 목적:**
  - 인터넷 창을 띄워놓고 작업하다 보면 해야 할 일을 잊어버리거나, 별도의 투두리스트 앱을 켜야 하는 번거로움이 있음.
  - 브라우저 안에서 마우스 우클릭만으로 할 일을 빠르게 등록하고, 팝업 창을 통해 직관적으로 완료 체크 및 삭제를 할 수 있는 초간편 할 일 관리 도구를 제공하고자 함.

---

## ② 기능 정의

- **우클릭 컨텍스트 메뉴 생성:**
  - 웹페이지에서 중요 텍스트를 드래그한 후 우클릭하거나, 빈 화면을 우클릭했을 때 `할 일(TODO) 추가` 메뉴 노출.
- **백그라운드 데이터 처리:**
  - `background.js`에서 우클릭 메뉴 클릭 이벤트를 감지.
  - 사용자가 드래그한 텍스트가 있다면 해당 텍스트를 할 일 내용으로 가져오고(50자 초과 시 말줄임표 처리), 없다면 기본 값("새로운 할 일")으로 백그라운드에서 데이터를 가공 및 저장 처리.
- **데이터 지속성 (저장):**
  - `chrome.storage.local` API를 사용하여 브라우저를 종료하거나 컴퓨터를 재부팅해도 추가한 할 일 목록이 그대로 유지되도록 구현.
  - **[데이터 구조 상세 명세]**
    ```json
    [
      {
        "id": 1716000000000,
        "text": "사용자가 드래그한 할 일 텍스트",
        "completed": false,
        "url": "https://example.com/current-page"
      }
    ]
    ```
- **팝업 UI 및 할 일 관리 (고도화):**
  - **실시간 디지털시계:** 팝업창 상단에 가시성 높은 적당한 크기의 실시간 시계(HH:MM:SS)를 출력하여 유저의 시간 관리를 지원.
  - **완료 체크:** 팝업 창에서 할 일 왼쪽의 체크박스를 누르면 취소선이 그어지며 완료 상태 변환.
  - **삭제 및 초기화:** 할 일 우측의 'X' 버튼을 통한 개별 삭제 및 헤더의 `전체 삭제 🧹` 버튼을 통한 전체 초기화 기능 구현.
  - **바탕화면 독립형 앱(PWA) 연동:** 팝업 하단에 외부 바로가기 인터페이스를 모사하여 향후 브라우저 종속 탈피 및 바탕화면 독립 실행을 위한 확장성 확보.
- **[신규 고도화 기능 4종]**
  1. **🔍 실시간 키워드 검색 및 필터링 (Search & Filter):** 팝업창 상단에 실시간 검색 바를 도입하여, 입력된 키워드에 맞는 ToDo 항목만 리스트에 동적으로 필터링 출력.
  2. **🖨️ CSV 파일 데이터 내보내기 (Data Export):** 팝업창에 축적된 ToDo 리스트 데이터(내용, 완료 여부, 원본 URL)를 BOM(Byte Order Mark)이 포함된 엑셀 호환 CSV 파일로 로컬 PC에 즉시 다운로드.
  3. **🌙 사용자 정의 다크 모드 (Dark Mode):** 야간 웹서핑 유저의 시각적 피로도를 낮추기 위해 토글 스위치 형태의 다크 테마를 제공하고, 해당 설정을 로컬 스토리지에 영구 저장.
  4. **🔗 클립보드 원클릭 복사 (Copy to Clipboard):** 각 ToDo 항목 옆에 복사 아이콘 버튼을 배치하여, 클릭 한 번으로 할 일 텍스트와 출처 URL 정보를 클립보드에 포맷팅하여 복사.

---

## ③ UX 설계 (사용자 시나리오)

1. **할 일 등록:** 사용자가 웹 서핑이나 인강 수강 중 중요 키워드를 드래그한 뒤 우클릭하여 `할 일(TODO) 추가`를 누르면 현재 탭의 URL 정보와 함께 백그라운드 스토리지에 자동 저장된다.
2. **할 일 확인 및 시계 활용:** 브라우저 툴바의 `WebToDo` 아이콘을 클릭하여 팝업창을 연다. 상단에 띄워진 실시간 시계로 현재 시각을 인지함과 동시에, 수많은 할 일 중 원하는 키워드를 상단 검색창에 쳐서 빠르게 필터링한다. 저장된 투두리스트의 파란색 링크를 클릭해 스크랩했던 원본 웹사이트로 새 탭 이동한다.
3. **콘텐츠 활용 및 테마 변경:** 밤늦게 작업할 경우 우측 상단의 테마 스위치를 눌러 다크 모드로 전환하여 시력을 보호한다. 중요한 태스크는 클립보드 복사 버튼을 눌러 노션이나 카카오톡 공유 문서에 즉시 붙여넣는다.
4. **완료 및 관리:** 마친 태스크는 체크박스로 완료 처리(취소선)하고, 불필요한 목록은 개별 삭제하거나 전체 삭제 버튼을 눌러 리스트를 비운다. 모아둔 데이터를 백업하고 싶을 때는 `CSV 내보내기 💾` 버튼을 활용한다.

---

## ④ 기술 명세

- **개발 환경:** VS Code (Visual Studio Code)
- **사용 기술:** HTML5, CSS3, JavaScript (Vanilla JS)
- **크롬 확장 프로그램 주요 파일 구조:**
  - `manifest.json`: 확장 프로그램의 기본 메타데이터 설정 및 `contextMenus`, `storage` 권한 필터링.
  - `background.js`: 브라우저 실행 시 컨텍스트 메뉴를 안전하게 제거 후 재빌드하고, 우클릭 이벤트를 캐치하여 URL과 긴 문장 예외 처리(50자 제한)가 반영된 텍스트 데이터를 스토리지 배열에 누적한 후 DevTools에 성공 로그를 출력 및 실시간 브로드캐스팅을 전송하는 스크립트.
  - `popup.html` / `popup.css`: 적당한 크기의 실시간 시계 영역, 테마 토글, 검색 바, 내보내기 버튼, 전체 삭제 헤더, 커스텀 스크롤바 리스트 렌더링 스페이스, 하단 PWA 버튼이 다크 모드 가변 색상 테마와 조화롭게 배치된 고급 디자인 UI.
  - `popup.js`: 실시간 타이머(`setInterval`) 오차 방지 구동, 다크모드/검색필터/CSV다운로드(Blob URL 매핑)/클립보드 API 연동, 데이터 체크/삭제/전체 초기화 렌더링 및 서비스 워커의 실시간 브로드캐스팅 수신 담당.
- **주요 API 및 시스템 권한:**
  - `contextMenus` 권한: 마우스 우클릭 메뉴 등록 및 `chrome.contextMenus.create()` 사용
  - `storage` 권한: 투두리스트 데이터 및 유저 테마 설정을 영구 저장하기 위해 `chrome.storage.local` 사용
  - `chrome.runtime.sendMessage()` / `onMessage.addListener()` : 서비스 워커와 팝업창 간 실시간 데이터 동기화 신호 송수신
  - `navigator.clipboard.writeText()` : 클립보드 복사 기능을 위한 비동기 웹 API 사용
  - `URL.createObjectURL(Blob)` : 메모리 상의 CSV 데이터를 다운로드 주소로 매핑하기 위한 웹 API 사용

---

## ⑤ 기대 효과 및 향후 고도화 계획

- **기대 효과:**
  - 별도의 창 전환 없이 웹서핑 흐름을 유지하며 컨텍스트 스위칭 비용 최소화.
  - 할 일을 수집한 원본 주소를 링크로 제공함으로써 정보의 출처 추적성 대폭 향상.
  - 다크모드, 검색, 복사, 내보내기 등 상용 앱 수준의 편의 기능을 올인원으로 내장하여 사용자 경험 완성도 극대화.
- **차기 고도화 로드맵:**
  - 미완료 할 일 개수를 브라우저 툴바 아이콘 위에 숫자로 실시간 표기하는 배지(Badge) 카운트 기능 연동 (`chrome.action.setBadgeText`).
  - 외부 인공지능 요약 API를 전격 연동하여, 단순 텍스트 저장을 넘어 유저가 드래그한 문장의 핵심을 3줄 요약해 주는 'AI 스마트 클리퍼' 기능 확장 패치 예정.
